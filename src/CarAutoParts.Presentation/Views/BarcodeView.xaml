<UserControl x:Class="CarAutoParts.Presentation.Views.BarcodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Barcode Generator" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="Generate barcodes for products and labels"
                       Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" Margin="0,4,0,0" />
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource GlassCardStyle}" MaxWidth="560" HorizontalAlignment="Left">
            <StackPanel>
                <TextBox materialDesign:HintAssist.Hint="Barcode text"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Text="{Binding BarcodeText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,12" />
                <Button Content="Generate" Style="{StaticResource PrimaryActionButton}"
                        Command="{Binding GenerateCommand}" HorizontalAlignment="Left" />
                <Border Background="White" Padding="12" Margin="0,16,0,0"
                        HorizontalAlignment="Left" CornerRadius="8"
                        BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                    <Image Source="{Binding BarcodeImage}" Stretch="None" MinHeight="80" MinWidth="200" />
                </Border>
                <TextBlock Text="{Binding StatusMessage}" Margin="0,16,0,0"
                           Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

