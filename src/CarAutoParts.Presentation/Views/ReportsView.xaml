<UserControl x:Class="CarAutoParts.Presentation.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:CarAutoParts.Presentation.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <views:EntityModuleView>
        <views:EntityModuleView.MainContent>
            <StackPanel Visibility="{Binding PlaceholderMessage, Converter={StaticResource NullToVis}, ConverterParameter=invert}">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                    <DatePicker Width="160" Margin="0,0,12,0"
                                materialDesign:HintAssist.Hint="Export from"
                                SelectedDate="{Binding ExportFromDate}" />
                    <DatePicker Width="160" Margin="0,0,12,0"
                                materialDesign:HintAssist.Hint="Export to"
                                SelectedDate="{Binding ExportToDate}" />
                    <TextBlock VerticalAlignment="Center"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               Text="Date range applies to sales, purchase, and profit exports." />
                </StackPanel>
                <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource ModuleListItemBorderStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="15" />
                                    <TextBlock Text="{Binding Description}"
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               TextWrapping="Wrap" Margin="0,4,0,0" />
                                </StackPanel>
                                <Button Grid.Column="1" Content="Export" Style="{StaticResource PrimaryActionButton}"
                                        VerticalAlignment="Center"
                                        Command="{Binding DataContext.RunReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            </StackPanel>
        </views:EntityModuleView.MainContent>
    </views:EntityModuleView>
</UserControl>
