<UserControl x:Class="CarAutoParts.Presentation.Views.AnalyticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:CarAutoParts.Presentation.Controls"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <controls:LoadingShimmer IsLoading="{Binding IsBusy}" />
        <ScrollViewer VerticalScrollBarVisibility="Auto"
                      Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
            <StackPanel>
                <Grid Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Analytics" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="Business insights and trend analysis"
                                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="13" Margin="0,4,0,0" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top">
                        <DatePicker SelectedDate="{Binding FromDate}" Width="130" Margin="0,0,8,0" />
                        <DatePicker SelectedDate="{Binding ToDate}" Width="130" Margin="0,0,8,0" />
                        <Button Content="Apply" Style="{StaticResource GhostActionButton}" Margin="0,0,8,0"
                                Command="{Binding ApplyDateFilterCommand}" />
                        <Button Content="Refresh" Style="{StaticResource GhostActionButton}"
                                Command="{Binding RefreshCommand}" />
                    </StackPanel>
                </Grid>

                <Border Style="{StaticResource GlassCardStyle}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Key Insights" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,12" />
                        <ItemsControl ItemsSource="{Binding Insights}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource ModuleListItemBorderStyle}" Margin="0,0,0,6">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="LightbulbOnOutline" Width="16" Height="16"
                                                                     Foreground="{StaticResource WarningBrush}"
                                                                     Margin="0,2,10,0" />
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap" />
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource GlassCardStyle}">
                    <StackPanel>
                        <TextBlock Text="Trend Chart" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,12" />
                        <lvc:CartesianChart Series="{Binding TrendSeries}" XAxes="{Binding TrendXAxes}" Height="300" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

