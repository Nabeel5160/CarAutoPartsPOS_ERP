<UserControl x:Class="CarAutoParts.Presentation.Controls.KpiCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root">
    <Border Style="{StaticResource GlassCardStyle}" MinWidth="180" MinHeight="110" Margin="8">
        <Border.InputBindings>
            <MouseBinding MouseAction="LeftClick"
                          Command="{Binding Command, ElementName=Root}" />
        </Border.InputBindings>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Title, ElementName=Root}"
                           Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                <TextBlock Text="{Binding Value, ElementName=Root}"
                           Style="{StaticResource PageTitleStyle}" FontSize="22" Margin="0,6,0,0" />
            </StackPanel>
            <Border Grid.Column="1" Width="44" Height="44" CornerRadius="22"
                    Background="{Binding AccentBrush, ElementName=Root}" Opacity="0.15" />
            <materialDesign:PackIcon Grid.Column="1" Kind="{Binding Icon, ElementName=Root}"
                                     Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center"
                                     Foreground="{Binding AccentBrush, ElementName=Root}" />
        </Grid>
    </Border>
</UserControl>
