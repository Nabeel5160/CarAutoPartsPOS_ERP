<UserControl x:Class="CarAutoParts.Presentation.Controls.SearchBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root">
    <TextBox Style="{StaticResource MaterialDesignOutlinedTextBox}"
             materialDesign:HintAssist.Hint="{Binding Placeholder, ElementName=Root}"
             materialDesign:TextFieldAssist.HasClearButton="True"
             Text="{Binding SearchText, ElementName=Root, UpdateSourceTrigger=PropertyChanged}">
        <TextBox.InputBindings>
            <KeyBinding Key="Enter" Command="{Binding SearchCommand, ElementName=Root}" />
        </TextBox.InputBindings>
    </TextBox>
</UserControl>
